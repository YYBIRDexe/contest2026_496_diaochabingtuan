# 四车编队当前状态

检查日期：2026-09-19。

## 车端状态

- 四车 `formation-agent@robotN.service` 为 active。
- 四车 `formation-command-bridge@robotN.service` 为 active。
- 每车只有一个 `reconfiguration_agent.py` 进程。
- 四车均没有运动许可文件。
- 四车均没有 fleet endpoint 残留进程。
- robot1 与 robot4 使用 `mentorpi-localization-vendor-domain.service`。
- robot2 与 robot3 使用 `mentorpi-localization.service`。
- 四车 AMCL、map_server、雷达和电机安全桥保持运行。

## 网络状态

- 本机到 `192.168.1.1` 二十次 ping：0% 丢包。
- 本机到 `192.168.1.201–204` 各二十次 ping：0% 丢包。
- agent 空闲发布频率为 2 Hz，运动发布频率为 10 Hz。
- 闭合路线由车端根据统一 `trajectory_id` 自主跟踪，路线心跳中断约 2 秒后自动停止。

## 代码状态

- agent 停止流程支持 SIGINT、SIGTERM 和超时结束。
- fleet endpoint telemetry 使用原生 JSON 类型。
- command bridge 可以读取 root 创建的只读短期许可。
- 正式任务等待四个 bridge 确认许可，再发布完整 trajectory。
- planner 会检查完整候选路线并继续搜索其余安全候选路线。
- 新增 `--motion-mode synchronized`，本地同步路线回归测试通过。
- 车端路线跟踪使用 `0.28 m` 前视距离，连续通过拐角并减少局部降速。
- 待安装的车端配置将使用 `limits.max_linear_mps=0.875` 和 `limits.max_acceleration_mps2=2.0`。
- 本地九项回归测试全部通过。
- 三角编队闭合路线仿真以 `0.875 m/s`运行，单圈约 `15.06 秒`，最小车距为 `0.404 米`。
- 地图线段检查使用地图分辨率采样，A*单次搜索缓存线段安全结果和网格坐标；现场方阵规划耗时约 `5.4 秒`。

## 现场任务状态

当前发布包已经具备正式任务入口、车端自主路线跟踪和完整停止保护。规划、任务状态、agent、telemetry、自动定位、扫描定位、命令桥和命令限制文件已经安装到四台小车。

四车已经通过地图与代码校验、服务状态、agent 单实例、AMCL、雷达、零输出许可握手和许可删除检查。电脑到路由器及四台小车各二十次 ping 均为 0% 丢包。原地检查没有发送路线或移动速度。实际速度与路线完成情况仍需场地运动测试。
